import SwiftUI

struct SettingsScreen: View {
    var body: some View { NavigationStack { ZStack { SecondTheme.background.ignoresSafeArea(); ScrollView { VStack(spacing:16) { ScreenHeader(title:"Settings", subtitle:"Privacy, data, and future integrations"); AppCard(title:"Data Mode") { VStack(spacing:10) { MetricRow(label:"Current build", value:"iPhone-only"); MetricRow(label:"Watch integration", value:"Planned"); MetricRow(label:"HealthKit", value:"Planned"); MetricRow(label:"Cloud sync", value:"Off"); MetricRow(label:"Storage", value:"Local-first") } }.padding(.horizontal); AppCard(title:"Architecture Notes") { VStack(alignment:.leading, spacing:8) { Text("Live biometric data should come from Apple Watch using HKLiveWorkoutBuilder."); Text("HealthKit queries should be used for durable backfill and reconciliation, not primary live UI."); Text("WatchConnectivity should send source timestamps, not arrival timestamps."); Text("Transcript and biometric events should align by session-relative time windows.") }.font(.caption).foregroundStyle(SecondTheme.secondaryText) }.padding(.horizontal); AppCard(title:"Security") { VStack(spacing:10) { MetricRow(label:"HIPAA compliant", value:"No"); MetricRow(label:"Medical diagnosis", value:"No"); MetricRow(label:"Camera capture", value:"No"); MetricRow(label:"Data export", value:"Planned"); MetricRow(label:"Delete all data", value:"Planned") } }.padding(.horizontal) }.padding(.bottom,24) } } } }
}
